# Todo

## Search

Make it so that the search doesnt show `#{id}` but rather `#{normalized nr. in list}` (so ignoring tasks which are filtered out from the numbering)

## Material change

The material change interface does not have a cancel field with a red glass pane which we should add.

## Wrapped multiple tags

In the multiple tags argument (`namespace:tag,namespace:tag2`) the char '-' is confused with a seperator, while it can actually just be part of the namespace/tag name
